
  # Car Insurance App Flow (Copy)

  This is a code bundle for Car Insurance App Flow (Copy). The original project is available at https://www.figma.com/design/firdavVob4WbvmnvGzEeS8/Car-Insurance-App-Flow--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  